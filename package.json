{
  "name": "kuronest-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx json-server db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}